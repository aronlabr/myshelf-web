<h2 class="page-header"><%= author.name %></h2>
<div class="btn-row">
    <a class="btn btn-primary" href="/authors/<%= author.id %>/edit">Editar</a>
    <%- include('../partials/deleteForm.ejs', {url: `/authors/${author.id}`}) %>
</div>
<% if (Object.keys(booksByAuthor).length > 0) { %>
    <h2 class="page-header">Libros del Autor</h2>
    <%- include('../partials/bookGrid.ejs', {books: booksByAuthor}) %>
<% } %>